	/*(function(){

    const _colors = {
            primary : "#445500",
            primaryDark : "#222700",
            accent : "#009944",
            text : "#222288",
            background : "#daf0e0"
        };


    exports.init = function(options){

        exports.all = _colors;

    };

    exports.get = function(id){

        if (id !== undefined && id !== null && typeof(id) === 'string'){

            if (_colors && _colors[id]){
                return _colors[id];

            } else {
                return '#000000';

            }

        } else {
            return '#000000';

        }

    };

    exports.all = _colors;

}());*/

  const _colors = {
  			colorLocation : "#f3ffe6",
  			translucentLocation : "#55f3ffe6",
  			colorNotifications : "#fff8e6",
  			colorWeb : "#ffe6e6",
  			colorAdvanced : "#e6f6ff",
  			colorSettings : "#dff7ec",
            primary : "#555500",
            primaryDark : "#222200",
            accent : "#7d86ff",
            text : "#222288",
            background : "#eee",
            translucent : "#3fcacaca"
        };
        
exports.all = _colors;